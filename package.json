{
  "scripts": {
    "start": "node index.js",
    "postinstall": "./node_modules/.bin/grunt",
    "clean": "./node_modules/.bin/grunt clean:dist",
    "clean:all": "./node_modules/.bin/grunt clean:all"
  },
  "dependencies": {
    "express": "^4.7.2",
    "grunt": "^0.4.5",
    "grunt-cli": "^0.1.13",
    "grunt-contrib-clean": "^0.6.0",
    "grunt-contrib-copy": "^0.5.0",
    "grunt-html-build": "^0.3.2",
    "newrelic": "^1.10.0"
  }
}
